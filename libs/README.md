### Libs used for the project

This folder is for storing the A2UI libs or extra ones

Must be build before running

a2a_agents folder nests the latest `a2ui` python extension not released yet.

`lit` and `web_core` folders are for the client side to render components and unfold A2UI events